image boot.vfat {
	vfat {
		label = "piClock"
		files = {
#BOOT_FILES#
		}
	}

	size = 256M
}

image sdcard.img {
	hdimage {
	}

	partition boot {
		partition-type = 0xC
		bootable = "true"
		image = "boot.vfat"
	}
}
