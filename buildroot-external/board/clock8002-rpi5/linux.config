# clock8002 Pi 5 kernel config fragment
# brcmuart probe triggers BCM2712 pinctrl SError during early boot;
# the clock app uses PL011 UARTs (uart1-3 dtoverlay), not brcmuart.
CONFIG_SERIAL_BCM_BRCMUART=n
